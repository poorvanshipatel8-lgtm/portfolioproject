/** Project cards initialization (simplified - case study details removed) */

export function initProjects() {
  // Project cards now display dashboard preview image and link only
  // No expandable case study functionality
}
